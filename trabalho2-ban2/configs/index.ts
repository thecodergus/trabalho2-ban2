import mongoose, {mongo_url} from "./mongoose";
import session from "./session";
// import { my_Strategy, passport_local_config, passport_local_options } from "./passport";


export {
    mongoose,
    mongo_url,
    session,
    // my_Strategy,
    // passport_local_config,
    // passport_local_options
}